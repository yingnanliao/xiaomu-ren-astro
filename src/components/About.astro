---
const MAPS_URL = 'https://www.google.com/maps/search/?api=1&query=台中市霧峰區新生路126號光復新村';
---

<section id="about" style="background:var(--color-bg);">
  <div class="max-w-7xl mx-auto px-5 sm:px-6 lg:px-12 py-16 sm:py-20"
    style="border-bottom:1px solid var(--color-border);">

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-16 items-center">

      <!-- 文字 -->
      <div class="reveal-left">
        <span class="tag">About Us</span>
        <h2 class="mt-4 text-3xl sm:text-4xl lg:text-5xl font-black" style="color:var(--color-ink);">
          從一頭羊<br/>
          <span style="color:var(--color-sage);">到您的餐桌</span>
        </h2>
        <p class="mt-5 leading-loose text-sm" style="color:var(--color-ink-muted);">
          2012 年，創辦人陳先生在台中霧峰山區接手家族羊場，
          以有機農業理念重新出發。十年來，他用對土地最深的
          敬重，換來每一杯純粹無添加的鮮羊奶。
        </p>
        <p class="mt-3 leading-loose text-sm" style="color:var(--color-ink-muted);">
          2021 年進駐光復新村青創基地，讓更多人能親身感受
          牧場的溫度。我們不追求規模，只專注在品質與誠信。
        </p>
        <!-- 地址 Google Maps 連結 -->
        <a href={MAPS_URL} target="_blank" rel="noopener"
          class="mt-5 inline-flex items-start gap-2 text-xs transition-colors"
          style="color:var(--color-sage);"
          onmouseover="this.style.color='var(--color-ink)'"
          onmouseout="this.style.color='var(--color-sage)'">
          <svg class="mt-0.5 flex-shrink-0" width="13" height="13" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
              d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
          </svg>
          台中市霧峰區新生路 126 號（光復新村）→ 導航前往
        </a>
        <div class="mt-7">
          <a href="#timeline" class="btn-outline-dark" style="padding:10px 24px; font-size:.72rem;">
            品牌故事 →
          </a>
        </div>
      </div>

      <!-- 特色格 -->
      <div class="reveal-right grid grid-cols-2 gap-3 sm:gap-4">
        {[
          { icon:'🌿', title:'自然放牧', desc:'青山草地自由生長，不施打生長激素' },
          { icon:'🥛', title:'低乳糖', desc:'脂肪球僅牛奶 1/3，腸胃好吸收' },
          { icon:'🌡', title:'巴氏殺菌', desc:'凌晨一點完成，保留最多營養' },
          { icon:'🚚', title:'冷鏈宅配', desc:'週三、週五配送台中市區到府' },
        ].map((f, i) => (
          <div class="p-5 sm:p-6"
            style={`border:1px solid var(--color-border); background:${i%2===1 ? 'var(--color-bg-2)':'var(--color-bg)'}`}>
            <div class="text-2xl mb-2.5">{f.icon}</div>
            <div class="text-sm font-black mb-1" style="color:var(--color-ink);">{f.title}</div>
            <div class="text-xs leading-relaxed" style="color:var(--color-ink-muted);">{f.desc}</div>
          </div>
        ))}
      </div>
    </div>
  </div>

  <!-- 引言條 -->
  <div class="py-10 sm:py-12" style="background:var(--color-sage-pale); border-bottom:1px solid var(--color-border);">
    <div class="max-w-7xl mx-auto px-5 sm:px-6 lg:px-12 reveal">
      <p class="text-xl sm:text-2xl lg:text-3xl font-black text-center leading-snug"
        style="color:var(--color-sage); font-family:var(--font-serif);">
        「從一頭羊的日常，到一個家庭的健康」
      </p>
    </div>
  </div>
</section>
